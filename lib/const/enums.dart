enum AuthFormType {
  register,
  login,
  resetPassword,
  resetTokenSended,
  typeNewPassword
}

enum InitialSetupStatus {
  choosingCourse,
}

enum QuizType { learning, review }

enum QuizAnswerStatus { success, failure }

class Achievement {
  Achievement(
      {required this.id,
      required this.name,
      required this.description,
      required this.goal});

  int id;
  String name;
  String description;
  int goal;
}
